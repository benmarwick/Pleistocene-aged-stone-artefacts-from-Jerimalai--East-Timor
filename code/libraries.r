##  ---- libraries


# libraries need to be in a .R file so packrat can find them

require(ggplot2)
require(reshape2)
require(plyr)
require(dplyr)
require(vcd)
require(rjags)
require(coda)
require(BEST)
require(lattice)
require(xtable)
require(data.table)

